///*First WHILE program*/
#include <stdio.h>

main(){
	int power = 2;

	while (power <= 1000) { //while 
		power = power * 2; //what to do
	}
	printf("First power of 2 over 1000 is %d\n", power);
	system("PAUSE");//console don't close after that command
	return 0; /*The program completed successfully*/
}
